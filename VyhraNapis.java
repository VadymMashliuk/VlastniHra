import greenfoot.*;  // (World, Actor, GreenfootImage, Greenfoot and MouseInfo)

/**
 * Write a description of class VyhraNapis here.
 * 
 * @author (your name) 
 * @version (a version number or a date)
 */
public class VyhraNapis extends Actor
{
    /**
     * Act - do whatever the VyhraNapis wants to do. This method is called whenever
     * the 'Act' or 'Run' button gets pressed in the environment.
     */
    public VyhraNapis()
    {
        GreenfootImage img = new GreenfootImage(getImage());
        img.scale(478, 351);
        setImage(img);
    }
}
